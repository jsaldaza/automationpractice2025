name: Serenity BDD Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Java 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: 17

      - name: Set up Chrome
        uses: browser-actions/setup-chrome@v1

      - name: Confirm Chrome installation
        run: |
          echo "Chrome binary path: $CHROME_BIN"
          $CHROME_BIN --version

      - name: Set environment variables for headless Chrome
        run: |
          echo "CHROME_BIN=$(which google-chrome)" >> $GITHUB_ENV
          echo "headless.mode=true" >> $GITHUB_ENV

      - name: Grant execute permission to Gradle Wrapper
        run: chmod +x ./gradlew

      - name: Cache Gradle packages
        uses: actions/cache@v3
        with:
          path: ~/.gradle/caches
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Run Serenity Tests (Headless Chrome)
        run: ./gradlew clean test --no-daemon

      - name: Upload Test Reports as artifacts
        uses: actions/upload-artifact@v3
        with:
          name: test-reports
          path: build/reports/tests/test/index.html  # Ajusta esta ruta si el reporte tiene otro nombre o ubicación

      - name: Archive Serenity Report
        uses: actions/upload-artifact@main
        with:
          name: serenity-report
          path: target/site/serenity
